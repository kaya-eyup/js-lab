// div elementine konumlan


// bir  fonksiyon oluştur (click)

// dive fonksiyonu ekleyip PointerEvent değerlerini incele

// parametrelerini incele:

// target, hangi elementin eventi aldığını söyler. // BURADA HATA VAR.
// timeStamp, eventin ne zaman tetiklendiğini söyler.
// clientX ve clientY,  event esnasında sayfanın neresinde konumlandığını söyler (windowa göre).
// offSetX ve offSetY,  event esnasında targetın neresinde konumlandığını söyler.

// tek elemente event bağlayacaksan addEventListener içine fonksiyonu yazabilirsin
// preventDefault, etiketin default özelliğini engeller. sayfanın yenilenmemesi gereken durumlarda kullanabilisin